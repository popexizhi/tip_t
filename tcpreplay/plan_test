#!/bin/bash
#all=(30 40 45 50 55 60)
all=(50 50)
type=$1
if [ "${type}" == "" ];then
    type="capture"
fi
for i in ${all[*]} 
do
    cur=`date +%Y%m%d_%H%M%S`
    testname="DNSmbp${i}_${cur}_${type}"
    echo `bash start_test_${type} ${testname} ${i}`
    echo "sleep 5min wait next test for io******************"
    sleep 30
    echo "******************"
done
